export default {
  customClass: '', // 自定义样式类，字符串形式（''）
  customStyle: {}, // 自定义样式，对象形式（默认值：{}）
  borderTop: '1px solid #e6e6e6', // 顶部边框样式，如需去掉，设置'none'
  safeArea: true, // 是否开启安全区域
  background: '#ffffff', // 背景
  safeAreaBgColor: '#ffffff', // 安全区域背景颜色
  height: '', // 建议传值，不传的时候，自动根据内容撑开
  fixed: false, // 是否fixed定位
  padding: '17rpx 32rpx' // 内边距
}
