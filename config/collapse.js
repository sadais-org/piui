export default {
  value: null, // 当前展开面板的name，非手风琴模式：[<string | number>]，手风琴模式：string | number
  accordion: false, // 是否手风琴模式，默认false
  border: true, // 是否显示边框，默认true
  customClass: '', // 自定义样式类，字符串形式（''）
  customStyle: {} // 自定义样式，对象形式（默认值：{}）
}
