export default {
  length: '100%', // 长度，默认（'100%'）
  hairLine: true, // 是否显示细边框，默认（'true'）
  margin: '0', // 边距，默认（'0'）
  borderStyle: 'solid', // 线条的类型，默认（'solid'）
  color: '#e4e7ed', // 颜色
  direction: 'row' // 排列方向
}
